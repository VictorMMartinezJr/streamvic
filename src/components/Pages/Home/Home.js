import HomeCarousel from '../../Stateful/HomeCarousel/HomeCarousel'

const Home = (url) => {
    return (
        <section className='home'>
            <HomeCarousel />
        </section>
    )
}

export default Home